import 'package:flutter/material.dart';

class HiddenMotionPicturesPage extends StatelessWidget {
  const HiddenMotionPicturesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Hidden motion pictures'));
  }
}
